
const Loader = Wrapped => class Loader extends Component {
  constructor(props) {
    super(props);

    this.renderLoading = this.renderLoading.bind(this);
    this.renderError = this.renderError.bind(this);
  }

  renderLoading() {

  }

  renderError() {

  }

  render() {

    if(this)

    return (

    );
  }
}
