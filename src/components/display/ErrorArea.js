import { ErrorIconWithText } from 'components/utils/Icons'
import React, { Component } from 'react'

class ErrorArea extends Component {
    render() {
        return <ErrorIconWithText text="An error has occurred." />
    }
}

export default ErrorArea
